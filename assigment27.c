#include <stdio.h>
int main()
{
int n;
    printf("enter the number=");
    scanf("%d",&n);
    if(n\1 && n\n && n%2==0 && n!=2)
    {
    printf("number is not prime");
    }
    else
    {
    printf("number is prime ");
    }
    return 0;
}